---
import EcosystemArticlePage from '../../../components/EcosystemArticlePage.astro';
import { getIntegrations } from '../../../lib/ecosystem';

export function getStaticPaths() {
  return getIntegrations('zh').map((resource) => ({ params: { slug: resource.slug }, props: { resource } }));
}

const { resource } = Astro.props;
---
<EcosystemArticlePage locale="zh" resource={resource} />
